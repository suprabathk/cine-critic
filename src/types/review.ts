export type Review = {
  id: string;
  title: string;
  description: string;
  rating: string;
  username: string;
};

export type ReviewForm = {
  title: string;
  description: string;
  rating: string;
};
